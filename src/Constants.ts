enum Constants {
    FPS = 60,
    STEP_RATE = 1.5,
    FPS_TIME = 1 / FPS,
    PHYSICS_STEP = 1 / (FPS * STEP_RATE),
    ROLL_BALL_RATE = 60 / FPS,
    CANVAS_WIDTH = 1360,
    CANVAS_HEIGHT = 640
};

export default Constants;